import React, {Component, Form, FormControl, Button} from 'react';

class NavigationBar extends Component {
    constructor(props) {
        super(props);
    }

    render(){
        return (
            <div className="container-fluid">
            </div>
        );
    }
}

export default NavigationBar;
